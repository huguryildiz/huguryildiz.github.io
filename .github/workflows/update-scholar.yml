name: Update Google Scholar Metrics

on:
  schedule:
    - cron: "30 1 1 * *"   # Every month on day 1 at 01:30 UTC (after update-openalex)
  workflow_dispatch:

jobs:
  fetch:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install requests

      - name: Fetch Google Scholar metrics
        env:
          # Repo → Settings → Secrets → Actions → New repository secret
          # Name:  SERPAPI_KEY          Value: https://serpapi.com/manage-api-key
          # Name:  SCHOLAR_AUTHOR_ID    Value: nQwHS1gAAAAJ
          SERPAPI_KEY: ${{ secrets.SERPAPI_KEY }}
          SCHOLAR_AUTHOR_ID: ${{ secrets.SCHOLAR_AUTHOR_ID }}
        run: python scripts/fetch_scholar.py

      - name: Commit & push if changed
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git pull --rebase --autostash
          git add _data/scholar_metrics.json
          git diff --cached --quiet || git commit -m "chore: update Google Scholar metrics [skip ci]"
          git push
